<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- The API key lives in a separate (plain, not encrypted) Preferences
         DataStore; keep it out of cloud backups. -->
    <exclude domain="file" path="datastore/kararead_secrets.preferences_pb" />
</full-backup-content>
